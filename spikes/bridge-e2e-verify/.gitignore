# Generated by prepare.mjs: a copy of @goleo/bridge's build output. Committing it
# would let the copy drift from the package the spike is supposed to be verifying,
# which is the whole point of the spike.
frontend/dist/bridge/

# The built spike.
bridge-e2e-verify
bridge-e2e-verify.exe
