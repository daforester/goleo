<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

{{range .Perms.Permissions}}    <uses-permission android:name="{{.}}" />
{{end}}{{with .Perms.LegacyXML}}{{.}}
{{end}}{{range .Perms.Features}}    <uses-feature android:name="{{.}}" android:required="false" />
{{end}}
    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        {{if .HasIcon}}android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        {{end}}android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
